/*---------------------------------------------------------------------------*\
| =========                 |                                                 |
| \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox           |
|  \\    /   O peration     | Version:  1.4                                   |
|   \\  /    A nd           | Web:      http://www.openfoam.org               |
|    \\/     M anipulation  |                                                 |
\*---------------------------------------------------------------------------*/

FoamFile
{
    version         2.0;
    format          ascii;

    root            "";
    case            "";
    instance        "";
    local           "";

    class           dictionary;
    object          fvSchemes;
}

// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

ddtSchemes
{
    default Euler;
}
offCentre 0.5;

gradSchemes
{
    default         Gauss linear;
}

divSchemes
{
    default none;
    // for partitionedExnerFoam
/*    div(stable.flux,stable.theta)   Gauss linearUpwind linearUpwind;
    div(buoyant.flux,buoyant.theta) Gauss linearUpwind linearUpwind;
    div(stable.flux,stable.u)       Gauss linearUpwind linearUpwind;
    div(buoyant.flux,buoyant.u)     Gauss linearUpwind linearUpwind;
*/
    // for partitionedExnerFoamAdv
    sigmaRho Gauss linearUpwind linearUpwind;
    theta    Gauss linearUpwind linearUpwind;
    u        Gauss linearUpwind linearUpwind;
/*    div(stable.volFlux,stable.sigmaRho) Gauss linearUpwind linearUpwind;
    div(buoyant.volFlux,buoyant.sigmaRho) Gauss linearUpwind linearUpwind;
    div(stable.volFlux,stable.theta) Gauss linearUpwind linearUpwind;
    div(buoyant.volFlux,buoyant.theta) Gauss linearUpwind linearUpwind;
    div(stable.volFlux,stable.u) Gauss linearUpwind linearUpwind;
    div(buoyant.volFlux,buoyant.u) Gauss linearUpwind linearUpwind;*/
}

laplacianSchemes
{
    default         Gauss linear uncorrected;
}

interpolationSchemes
{
    default        none;
    // For initialisation
    interpolate(theta) linear;
    
    // for partitionedExnerFoamAdv
    sigmaRho        linear;
    sigma           linear;
    theta           linear;
    rho             linear;
    massTransfer    linear;
    convection      linear;
    Uf              linear;
/*    // for partitionedExnerFoam
    interpolate(stable.sigmaRho) linear; //Upwind stable.flux linearUpwind;
    interpolate(buoyant.sigmaRho) linear; //Upwind buoyant.flux linearUpwind;
    interpolate(stable.theta) linear; //Upwind stable.flux linearUpwind;
    interpolate(buoyant.theta) linear; //Upwind buoyant.flux linearUpwind;
    interpolate(stable.sigma) linear;
    interpolate(buoyant.sigma) linear;
    interpolate(rhoSum) linear;
    interpolate(convection(stable.flux,stable.u)) linear;
    interpolate(convection(buoyant.flux,buoyant.u)) linear;
    interpolate(stable.u) linear;
    interpolate(buoyant.u) linear;
    interpolate((stable.buoyant.massTransfer|stable.sigmaRho)) linear;
    interpolate((buoyant.stable.massTransfer|buoyant.sigmaRho)) linear;

    // for partitionedExnerFoamAdv
    convection linear;
    Uf         linear;
    interpolate(stable.rho) linear;
    interpolate(buoyant.rho) linear;*/
}

snGradSchemes
{
    default         none;
    snGrad(Exner)   uncorrected;
}

fluxRequired
{
    default         no;
    Exner;
}


// ************************************************************************* //
